# Base URL of the SubSense API.
# Dev: leave unset — the app falls back to http://127.0.0.1:8000/api.
# Production (Vercel): set this to your deployed API URL, e.g.
#   VITE_API_BASE=https://subsense-api.onrender.com/api
VITE_API_BASE=
